import { ArrayToDateConversionPipe } from './array-to-date-conversion.pipe';

describe('ArrayToDateConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToDateConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
